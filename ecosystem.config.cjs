// PM2 NO se usa para este proyecto — el servidor corre dentro de Docker.
// Este archivo es solo referencia histórica.
// Comando de inicio: docker compose up -d
// Comando de stop:  docker compose down
// Logs:             docker compose logs -f bliss-server
